# 贡献与文档写作规范

本项目以可验证的服务端工程知识为目标。文章应帮助读者理解适用边界、收集证据、执行变更并验证结果，而不是只给出一个命令或固定参数。

## 内容位置

- 已发布文章位于 `src/content/docs/<topic>/`。
- 每个一级主题使用 `index.md` 组织完整学习路径。
- `astro.config.mjs` 定义侧边栏顺序和分组。
- `docs/` 是未发布的历史归档，不应继续新增内容。

新增主题前先判断能否归入现有 MySQL、PostgreSQL、Redis、DuckDB、Linux、网络或可靠性章节。只有形成独立知识体系时才增加一级主题。

## 新增文章

文件名使用小写英文和连字符，Frontmatter 至少包含唯一且清晰的 `title` 与 `description`：

```markdown
---
title: PostgreSQL 查询执行计划
description: 使用 EXPLAIN 比较估算行数、实际行数、Buffers 和节点循环。
---
```

`description` 应直接说明文章解决的问题，避免“本文介绍”“一些笔记”等无检索价值的表达。

一篇生产专题通常包含：

1. 问题、适用范围和明确的不适用场景。
2. 工作原理或关键语义。
3. 最小但完整的命令、SQL 或配置示例。
4. 常见失败方式、资源成本和安全边界。
5. 诊断证据、验证步骤和恢复方式。
6. 与同主题或跨主题文章的必要链接。

文章不必机械使用同一组标题，但不能省略风险与验证，只展示成功路径。

## 修改与迁移

修改现有文章时保留稳定 slug，除非旧路径明显错误。必须改名时，应同步处理侧边栏、主题导读、站内链接和重定向。

从 `docs/` 迁移旧稿时，不要原样复制。先核对目标软件版本，重写 Frontmatter、术语、命令和生产边界，再加入主题导读与侧边栏。确认迁移后的文章完整覆盖有效内容后，才能另行决定是否删除旧稿。

拆分或合并文章时，优先按读者问题和操作边界划分，不按命令数量划分。两个短页面如果共享同一问题、风险和验证流程，通常应该合并；独立生命周期或并发语义则适合单独成篇。

## 技术准确性

- 写明主要适用的大版本，避免把特定小版本、云厂商或扩展行为描述为普遍结论。
- 参数、系统视图和工具输出可能变化时，提醒读者在目标版本验证。
- 区分规范定义、常见默认值、实现细节和经验值。
- 性能结论必须说明数据规模、分布、并发、缓存和存储环境。
- 涉及事务、复制、备份、DDL 或故障切换时，同时描述失败和恢复路径。

引用外部资料时优先使用官方文档和稳定地址。正文应独立表达核心结论，不能让外部链接承担必要说明。

## 示例安全

- 代码围栏语言标识使用小写，例如 `sql`、`bash`、`javascript`。
- 不提交真实域名、IP、账号、Token、Cookie、连接串或业务数据。
- 破坏性命令必须限定对象和前置条件，并说明备份、终止条件与恢复方法。
- 压力测试、抓包、详细日志和 Profiling 必须注明环境隔离、持续时间和输出限制。
- SQL 示例应显式说明是否会真实执行写入，未知写语句不要直接使用 `EXPLAIN ANALYZE`。

## 导航与链接

新增文章必须完成三处维护：

1. 加入 `astro.config.mjs` 中正确的主题和学习阶段。
2. 加入该主题的 `index.md`，并说明建议阅读位置。
3. 从最相关的现有文章建立必要交叉链接。

使用相对链接连接站内 Markdown 页面。链接目录路由时保留结尾 `/`，例如 `../reliability/timeout-retry/`。不要创建只能通过搜索发现的孤立页面。

## 语言与格式

- 中文正文使用准确、直接的工程语言，第一次出现的英文术语可以补充中文含义。
- 产品名保持官方写法，例如 PostgreSQL、MySQL、Redis、DuckDB、Linux。
- 命令、路径、配置名、SQL 标识符和状态码使用反引号。
- 标题描述问题或能力，避免“其他”“总结”“一些技巧”等模糊名称。
- 表格用于稳定映射和比较；包含长段说明时改用正文或列表。
- 不使用尾随空格，不手工写与主题无关的 HTML 样式。

## 本地验证

项目统一使用 Bun。提交前执行：

```bash
bun install --frozen-lockfile
bun run build
git diff --check
```

然后检查新增或修改页面：

- 标题、目录、表格和代码块在桌面与移动宽度下可读。
- 所有内部链接、静态资源和锚点可访问。
- 文章在主题侧边栏中只出现一次，前后页顺序合理。
- 示例不包含秘密信息，命令不会因占位符不清而误操作生产对象。

## 评审清单

- 文章回答了一个明确问题，且说明适用边界。
- 结论与目标版本一致，没有把默认值写成永久保证。
- 示例能够解释核心语义，但没有复制无关的大段输出。
- 风险、权限、资源成本、终止条件和恢复方式完整。
- 主题导读、侧边栏、交叉链接和 README 范围说明已同步。
- 完整构建通过，未引入失效链接或格式错误。
