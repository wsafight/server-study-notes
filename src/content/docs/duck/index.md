---
title: DuckDB 学习路径
description: 从嵌入式分析定位、文件查询、性能优化和应用集成系统学习 DuckDB。
---

DuckDB 适合在单机或单进程内执行扫描、连接、聚合和数据转换。它可以直接查询 Parquet、CSV、JSON 和对象存储，也能将结果持久化为数据库文件。

## 推荐顺序

1. [DuckDB 入门](./intro/)：理解嵌入式 OLAP 的定位与并发边界。
2. [查询与转换数据文件](./data-files/)：控制 Schema 推断、分区裁剪和输出格式。
3. [查询性能与资源控制](./query-optimization/)：通过执行计划、列裁剪、文件布局和内存限制减少工作量。
4. [在应用中嵌入 DuckDB](./embedding/)：管理连接、线程、数据库文件和发布生命周期。

## 适用与不适用

适合：本地分析、Notebook、批量数据转换、嵌入式报表、Parquet 数据湖临时查询，以及单机范围内的 ETL。

不适合：大量远程客户端、高并发小事务、依赖多节点在线扩展的服务，或多个独立进程持续写入同一数据库文件的场景。

## 实践建议

使用一组具有真实列数、分区和数据分布的文件做实验。分别记录读取字节、总耗时、峰值内存和临时磁盘，比较 CSV 与 Parquet、全列与列裁剪、单个大文件与大量小文件的差异。

DuckDB 迭代较快，扩展、配置项和客户端 API 应以项目锁定版本的文档为准。将版本、扩展来源和文件格式参数一同纳入可复现环境。
